{% extends 'base.html.twig' %}

{% block contenu %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <br>
    <h3 style="text-align:center;">Liste des stages proposés</h3>
    <br>

    {% for stageCourant in stages %}

    <a class="link" href="{{path('prostages_stage', {id:stageCourant.id})}}"><h5>{{ stageCourant.titre }}</h5></a>
    <h6>{{ stageCourant.Entreprise.nom }}</h6>
    <br>

    {% endfor %}
</div>
{% endblock %}
